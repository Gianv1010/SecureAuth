# Esercizio: recupero dalla cronologia

Sono stati creati tre commit:
1. **Initial commit working script** – contiene `main.py` funzionante.
2. **oopsies deletes everything** – rimuove i file.
3. **try to fix with a blank file** – reintroduce `main.py` vuoto.

🎯 Obiettivo: recupera la versione funzionante dal commit iniziale (usando `git restore`, `git checkout <commit> -- file`, oppure `revert`) in modo che l'autograding passi.
